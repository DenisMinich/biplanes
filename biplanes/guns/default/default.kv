<DefaultGun>:
    canvas.before:
        PushMatrix
        Rotate:
            angle: self.plane.angle
            axis: 0, 0, 1
            origin: self.plane.center
    canvas:
        Rectangle:
            size: self.plane.size
            pos: self.plane.pos
            texture: self.texture
    canvas.after:
        PopMatrix
